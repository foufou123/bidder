<div class ="jumbotron">
  <h2>Bidder is the best way to find reviews on local service providers.</h2>
  <h3>Explore the favorite plumbers, contractors, and handymen in your community.
  <br>Leave reviews and ratings for your fellow bidders.</h3>
</div>
<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Phone</th>
      <th>Location</th>
      <th>Zipcode</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @jobs.each do |job| %>
      <tr>
        <td><%= job.name %></td>
        <td><%= job.description %></td>
        <td><%= job.phone %></td>
        <td><%= job.location %></td>
        <td><%= job.zipcode %></td>
        <td><%= link_to 'Show', job, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_job_path(job), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', job, method: :delete, data: { confirm: 'Are you sure?', class: "btn btn-link" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Job', new_job_path %>
